# 拷贝漫画下载器

## 介绍
基于 reqwest 与 chromiumoxide 的浏览器版漫画爬虫。

> 本分支为浏览器内核版；纯命令行的 reqwest 版本见 `master` 分支。

## 构建
在根目录下运行：

```
cargo build --release
```

## 注意
- 需要本机安装 Chrome/Chromium 内核。
- 仅支持 windows8/10/11
